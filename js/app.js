const state={tab:"home",userStatus:"guest",notificationMode:"system",readContentIds:new Set(["c3"]),readNotificationIds:new Set(["n4"]),reactions:[...window.DEMO_DATA.reactions]};const data=window.DEMO_DATA;const app=document.querySelector("#app"),pageTitle=document.querySelector("#pageTitle"),memberStatus=document.querySelector("#memberStatus"),desktopBadge=document.querySelector("#desktopBadge"),mobileBadge=document.querySelector("#mobileBadge");function isMember(){return state.userStatus==="member"}function statusLabel(){return state.userStatus==="member"?"Member":state.userStatus==="expired"?"Expired":"Guest"}function unreadContents(){return data.contents.filter(c=>!state.readContentIds.has(c.id))}function unreadNotifications(){return data.notifications.filter(n=>!state.readNotificationIds.has(n.id))}function updateShell(t){pageTitle.textContent=t;memberStatus.textContent=statusLabel();const u=unreadNotifications().length>0;desktopBadge.classList.toggle("hidden",!u);mobileBadge.classList.toggle("hidden",!u)}function setTab(t){state.tab=t;document.querySelectorAll(".nav-item,.mobile-tab").forEach(b=>b.classList.toggle("active",b.dataset.tab===t));render()}function render(){if(state.tab==="home")return renderHome();if(state.tab==="contents")return renderContents();if(state.tab==="notifications")return renderNotifications();if(state.tab==="mypage")return renderMyPage()}function renderHome(){updateShell("Home");const u=unreadContents(),o=data.notifications.find(n=>n.type==="official");app.innerHTML=`<section class="hero"><p>${isMember()?"おかえりなさい":state.userStatus==="expired"?"期限が切れています":"ゲスト利用中"}</p><h2>30min. with you</h2><p>情報を見る場所から、推し活を逃さない場所へ。</p></section><h2 class="section-title">Dashboard</h2><div class="card-grid home-grid"><article class="card clickable" onclick="setTab('contents')"><div class="card-row"><div><h3>未読Contents</h3><p>${u.length?u[0].title:"未読はありません"}</p></div><span class="badge ${u.length?"":"hidden"}">${u.length}</span></div></article><article class="card clickable" onclick="setTab('notifications')"><div class="card-row"><div><h3>Notifications</h3><p>${unreadNotifications().length}件の未読通知</p></div><span class="badge danger">${unreadNotifications().length}</span></div></article><article class="card clickable" onclick="setTab('notifications')"><div><h3>News</h3><p>${o.title}</p></div></article></div>`}function renderContents(){updateShell("Contents");const m=[...new Set(data.contents.map(c=>c.month))];app.innerHTML=`<div class="chip-row"><span class="chip active">降順：最新→過去</span><span class="chip">年月見出し</span><span class="chip">reaction対応</span></div>${m.map(month=>`<div class="month-heading">${month}</div><div class="card-grid">${data.contents.filter(c=>c.month===month).map(contentCard).join("")}</div>`).join("")}`}function contentCard(item){const u=!state.readContentIds.has(item.id);return `<article class="card clickable" onclick="openContent('${item.id}')"><div class="card-row"><div><h3>${item.title}</h3><p>${isMember()?item.body.slice(0,58)+"…":"会員限定：タイトルのみ表示"}</p><div class="meta"><span>${item.date}</span><span>・</span><span>${item.category}</span></div></div><span class="badge ${u?"":"hidden"}">NEW</span></div></article>`}function openContent(id){const item=data.contents.find(c=>c.id===id);if(!isMember())return renderMemberRequired(item);state.readContentIds.add(id);data.notifications.filter(n=>n.type==="system"&&n.contentId===id).forEach(n=>state.readNotificationIds.add(n.id));updateShell("Detail");const mine=state.reactions.find(r=>r.contentId===id&&r.userId==="u-demo-001");app.innerHTML=`<button class="back-btn" onclick="renderContents()">← 戻る</button><div style="height:14px"></div><section class="detail-layout"><article class="card"><span class="badge">${item.category}</span><h2>${item.title}</h2><p class="meta">${item.date}</p><div class="detail-media"></div><p>${item.body}</p>${item.goodsInfo?`<div class="card" style="margin-top:16px">${item.goodsInfo}</div>`:""}</article><aside class="card"><h3>reaction</h3><p>この投稿への感想を運営へ送れます。他の会員には表示されません。</p><div style="height:12px"></div>${mine?`<div class="thanks">送信済み。ありがとう。</div><div style="height:10px"></div><p>${mine.message}</p>`:`<div class="reaction-box"><textarea id="reactionText" placeholder="感想を書く"></textarea><button class="primary-btn" onclick="sendReaction('${id}')">reactionを送る</button></div>`}</aside></section>`}function sendReaction(id){const input=document.querySelector("#reactionText"),message=input.value.trim();if(!message){input.focus();return}state.reactions.unshift({id:"r"+Date.now(),contentId:id,userId:"u-demo-001",userName:"demo member",date:new Date().toLocaleString("ja-JP"),message,deliveryStatus:"delivered"});const box=input.closest(".card");box.innerHTML=`<h3>reaction</h3><div class="thanks">ありがとう。届きました。</div><div style="height:10px"></div><p>${message}</p>`}function renderMemberRequired(item){updateShell("Member Required");app.innerHTML=`<button class="back-btn" onclick="renderContents()">← 戻る</button><div style="height:14px"></div><section class="card"><h2>会員登録が必要</h2><p>「${item.title}」の詳細閲覧には会員ログインが必要です。</p><div style="height:14px"></div><button class="primary-btn" onclick="setTab('mypage')">ログインへ</button></section>`}function renderNotifications(){updateShell("Notifications");const list=data.notifications.filter(n=>n.type===state.notificationMode);app.innerHTML=`<div class="chip-row"><button class="chip ${state.notificationMode==="system"?"active":""}" onclick="setNotificationMode('system')">system</button><button class="chip ${state.notificationMode==="official"?"active":""}" onclick="setNotificationMode('official')">official</button></div><button class="ghost-btn" onclick="markVisibleNotificationsRead()">表示中を一括既読</button><h2 class="section-title">${state.notificationMode}</h2><div class="card-grid">${list.map(n=>`<article class="card clickable" onclick="openNotification('${n.id}')"><div class="card-row"><div><h3>${n.title}</h3><p>${n.date}</p></div><span class="badge ${state.readNotificationIds.has(n.id)?"hidden":"danger"}">未読</span></div></article>`).join("")}</div>`}function setNotificationMode(m){state.notificationMode=m;renderNotifications()}function markVisibleNotificationsRead(){data.notifications.filter(n=>n.type===state.notificationMode).forEach(n=>state.readNotificationIds.add(n.id));renderNotifications()}function openNotification(id){const n=data.notifications.find(x=>x.id===id);state.readNotificationIds.add(id);if(n.contentId)return openContent(n.contentId);renderNotifications()}function renderMyPage(){updateShell("MyPage");app.innerHTML=`<section class="card"><h2>会員状態トグル</h2><p>v0.5では認証なし。開発用に状態だけ切り替えます。</p><div class="chip-row"><button class="chip ${state.userStatus==="guest"?"active":""}" onclick="setUserStatus('guest')">Guest</button><button class="chip ${state.userStatus==="member"?"active":""}" onclick="setUserStatus('member')">Member</button><button class="chip ${state.userStatus==="expired"?"active":""}" onclick="setUserStatus('expired')">Expired</button></div></section><h2 class="section-title">Account</h2>${state.userStatus==="member"?`<section class="card-grid"><article class="card"><h3>会員情報</h3><p>30min. Member / 有効期限：2026/12/31</p></article><article class="card"><h3>端末管理</h3><p>登録端末：2台まで。3台目ログイン時は最も古い端末を自動解除。</p></article></section>`:state.userStatus==="expired"?`<section class="card"><h3>会員期限が切れています</h3><p>ログアウトせず、権限だけGuest相当に降格しています。</p></section>`:`<section class="card"><h3>ゲストとして利用中</h3><p>Contents詳細とreactionは会員限定です。</p></section>`}`}function setUserStatus(s){state.userStatus=s;if(s==="member"){state.tab="home";document.querySelectorAll(".nav-item,.mobile-tab").forEach(b=>b.classList.toggle("active",b.dataset.tab==="home"))}render()}document.querySelectorAll(".nav-item,.mobile-tab").forEach(b=>b.addEventListener("click",()=>setTab(b.dataset.tab)));render();
